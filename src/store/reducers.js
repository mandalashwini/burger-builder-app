import * as actionType from './actions'
const initialState = {
    ingredients :null,
    total_price: 0,
}
const reducer = (state = initialState , action ) => {
    return state
}

export default reducer;